<?php

$continue = true;
echo '{';
while( $users->count()>0 && $continue )
{
    $user = $users->current();
    echo $user->toJson();
    $user = $users->next();
    if ( $users->valid() ){
        echo ',';
    }
    else{
        $continue = false;
    }
}
echo '}';
?>
